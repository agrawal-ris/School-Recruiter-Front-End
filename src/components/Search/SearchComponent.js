import React from 'react'
import SearchSchool from './SearchSchools'

export default class SearchComponent extends React.Component {
    render () {
        return (
            <SearchSchool/>
        )
    }
}